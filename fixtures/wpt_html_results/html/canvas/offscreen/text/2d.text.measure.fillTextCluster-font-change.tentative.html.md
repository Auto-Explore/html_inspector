# html/canvas/offscreen/text/2d.text.measure.fillTextCluster-font-change.tentative.html

Counts:
- errors: 0
- warnings: 1
- infos: 0

```json
{
  "format_version": 1,
  "file": "html/canvas/offscreen/text/2d.text.measure.fillTextCluster-font-change.tentative.html",
  "validated_html_truncated": false,
  "validated_html_max_bytes": 16384
}
```

Validated HTML:
```html
<!DOCTYPE html>
<!-- DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py. -->
<meta charset="UTF-8">
<link rel="match" href="2d.text.measure.fillTextCluster-font-change.tentative-expected.html">
<title>Canvas test: 2d.text.measure.fillTextCluster-font-change.tentative</title>
<h1>2d.text.measure.fillTextCluster-font-change.tentative</h1>
<p class="desc">Test that fillTextCluster() renders in the font used originally when the text was measured, even if the font set on the context has changed since.</p>
<canvas id="canvas" width="500" height="200">
  <p class="fallback">FAIL (fallback content)</p>
</canvas>
<script>
  const canvas = new OffscreenCanvas(500, 200);
  const ctx = canvas.getContext('2d');

  ctx.font = '50px sans-serif';
  const text = 'Hello ♦️ World!';
  let tm = ctx.measureText(text);
  const clusters = tm.getTextClusters();

  ctx.font = '80px serif';

  const x = 100;
  const y = 100;
  for (const cluster of clusters) {
      ctx.fillTextCluster(cluster, x, y);
  }

  const outputCanvas = document.getElementById("canvas");
  outputCanvas.getContext('2d').drawImage(canvas, 0, 0);
</script>
```

```json
{
  "messages": [
    {
      "category": "I18n",
      "code": "i18n.lang.missing",
      "message": "Consider adding a “lang” attribute to the “html” start tag to declare the language of this document.",
      "severity": "Warning",
      "span": null
    }
  ],
  "source_name": "html/canvas/offscreen/text/2d.text.measure.fillTextCluster-font-change.tentative.html"
}
```
