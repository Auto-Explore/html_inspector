# html/canvas/element/the-canvas-state/2d.state.saverestore.shadowOffsetY.html

Counts:
- errors: 0
- warnings: 1
- infos: 0

```json
{
  "format_version": 1,
  "file": "html/canvas/element/the-canvas-state/2d.state.saverestore.shadowOffsetY.html",
  "validated_html_truncated": false,
  "validated_html_max_bytes": 16384
}
```

Validated HTML:
```html
<!DOCTYPE html>
<!-- DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py. -->
<meta charset="UTF-8">
<title>Canvas test: 2d.state.saverestore.shadowOffsetY</title>
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<script src="/html/canvas/resources/canvas-tests.js"></script>
<link rel="stylesheet" href="/html/canvas/resources/canvas-tests.css">

<h1>2d.state.saverestore.shadowOffsetY</h1>

<script>

test(t => {
  const canvas = document.createElement('canvas');
  canvas.width = 300;
  canvas.height = 150;
  const ctx = canvas.getContext('2d');

  const old = ctx.shadowOffsetY;
  ctx.save();
  ctx.shadowOffsetY = 5;
  ctx.restore();
  _assertSame(ctx.shadowOffsetY, old, "ctx.shadowOffsetY", "old");
}, "save()/restore() restores shadowOffsetY, for a canvas of size (300, 150).");

test(t => {
  const canvas = document.createElement('canvas');
  canvas.width = 300;
  canvas.height = 150;
  const ctx = canvas.getContext('2d');

  ctx.shadowOffsetY = 5;
  const old = ctx.shadowOffsetY;
  // We're not interested in failures caused by get(set(x)) != x (e.g.
  // from rounding), so compare against `old` instead of 5.
  ctx.save();
  _assertSame(ctx.shadowOffsetY, old, "ctx.shadowOffsetY", "old");
  ctx.restore();
}, "save() does not modify shadowOffsetY, for a canvas of size (300, 150).");

test(t => {
  const canvas = document.createElement('canvas');
  canvas.width = 0;
  canvas.height = 0;
  const ctx = canvas.getContext('2d');

  const old = ctx.shadowOffsetY;
  ctx.save();
  ctx.shadowOffsetY = 5;
  ctx.restore();
  _assertSame(ctx.shadowOffsetY, old, "ctx.shadowOffsetY", "old");
}, "save()/restore() restores shadowOffsetY, for a canvas of size (0, 0).");

test(t => {
  const canvas = document.createElement('canvas');
  canvas.width = 0;
  canvas.height = 0;
  const ctx = canvas.getContext('2d');

  ctx.shadowOffsetY = 5;
  const old = ctx.shadowOffsetY;
  // We're not interested in failures caused by get(set(x)) != x (e.g.
  // from rounding), so compare against `old` instead of 5.
  ctx.save();
  _assertSame(ctx.shadowOffsetY, old, "ctx.shadowOffsetY", "old");
  ctx.restore();
}, "save() does not modify shadowOffsetY, for a canvas of size (0, 0).");

</script>
```

```json
{
  "messages": [
    {
      "category": "I18n",
      "code": "i18n.lang.missing",
      "message": "Consider adding a “lang” attribute to the “html” start tag to declare the language of this document.",
      "severity": "Warning",
      "span": null
    }
  ],
  "source_name": "html/canvas/element/the-canvas-state/2d.state.saverestore.shadowOffsetY.html"
}
```
