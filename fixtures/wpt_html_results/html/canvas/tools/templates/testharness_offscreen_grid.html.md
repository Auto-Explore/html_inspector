# html/canvas/tools/templates/testharness_offscreen_grid.html

Counts:
- errors: 0
- warnings: 1
- infos: 0

```json
{
  "format_version": 1,
  "file": "html/canvas/tools/templates/testharness_offscreen_grid.html",
  "validated_html_truncated": false,
  "validated_html_max_bytes": 16384
}
```

Validated HTML:
```html
<!DOCTYPE html>
<!-- DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py. -->
<meta charset="UTF-8">
<title>OffscreenCanvas test: {{ name }}</title>
{% if timeout %}<meta name="timeout" content="{{ timeout }}">
{% endif %}
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<script src="/html/canvas/resources/canvas-tests.js"></script>

<h1>{{ name }}</h1>

<script>
{% for variant in variants %}

{% if test_type == 'promise' %}
promise_test(async t => {
{% elif test_type == 'async' %}
async_test(t => {
{% else %}
test(t => {
{% endif %}
  const canvas = new OffscreenCanvas({{
      variant.size[0] }}, {{ variant.size[1] }});
  const ctx = canvas.getContext('2d'{%
      if variant.attributes %}, {{ variant.attributes }}{% endif %});

  {{ variant.code | trim | indent(2)}}
}, "{{ variant.desc | double_quote_escape }}");
{% endfor %}

</script>
```

```json
{
  "messages": [
    {
      "category": "I18n",
      "code": "i18n.lang.missing",
      "message": "Consider adding a “lang” attribute to the “html” start tag to declare the language of this document.",
      "severity": "Warning",
      "span": null
    }
  ],
  "source_name": "html/canvas/tools/templates/testharness_offscreen_grid.html"
}
```
