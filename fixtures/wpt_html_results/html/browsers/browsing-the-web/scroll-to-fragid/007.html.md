# html/browsers/browsing-the-web/scroll-to-fragid/007.html

Counts:
- errors: 0
- warnings: 1
- infos: 0

```json
{
  "format_version": 1,
  "file": "html/browsers/browsing-the-web/scroll-to-fragid/007.html",
  "validated_html_truncated": false,
  "validated_html_max_bytes": 16384
}
```

Validated HTML:
```html
<!doctype html>
<!-- this tests the spec as it hopefully will be once bug https://www.w3.org/Bugs/Public/show_bug.cgi?id=17155 is fixed -->
<title>Fragment Navigation: hashchange event multiple changes old/newURL</title>
<meta name=timeout content=long>
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<body>
<div id="log"></div>
<script>
var t = async_test();
t.step(function() {
  var original_url = location.href;
  assert_equals(location.hash, "", "Page must be loaded with no hash");

  var count = 0;

  location.hash = "test";

  hashes = [];

  addEventListener("hashchange",
                   t.step_func(function(e) {
                     if (count < 100) {
                       location.hash = "test" + count++;
                       hashes.push(location.hash);
                     } else if (count === 100) {
                       expected = [];
                       for (var i=0; i<100; i++) {
                          expected.push("#test" + i);
                       }
                       assert_array_equals(hashes, expected);
                       location.hash = "";
                       t.done();
                     }
                   }), true);
});
</script>
```

```json
{
  "messages": [
    {
      "category": "I18n",
      "code": "i18n.lang.missing",
      "message": "Consider adding a “lang” attribute to the “html” start tag to declare the language of this document.",
      "severity": "Warning",
      "span": null
    }
  ],
  "source_name": "html/browsers/browsing-the-web/scroll-to-fragid/007.html"
}
```
